const initialState = {
    strength: 0,
    health: 0,
    shield: 0,
    energy: 0,
    regeneration: 0,
};

const initialStatuses = {
    strength: false,
    health: false,
    shield: false,
    energy: false,
    regeneration: false,
};

export {
    initialState,
    initialStatuses,
};
